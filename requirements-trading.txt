# Optional trading / market data stack (install on top of base requirements.txt)
aiohttp>=3.9.0
websockets>=12.0
pandas>=2.0.0
numpy>=1.24.0

# Distributed backtests / heavy parallelism (optional)
# ray[default]>=2.9.0
